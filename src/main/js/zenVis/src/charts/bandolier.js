var zenoss = zenoss || {};

// TODO - create util for this?
zenoss.viz = zenoss.viz || {};
zenoss.viz.charts = zenoss.viz.charts || {};

(function(){
	"use strict";

	zenoss.viz.charts.bandolier = function(){
		this.on("update", function(data){
			// entry point into your chart
		});
	};

})();